version: 0.1
phases:
 install:
   commands:
     - echo "install step"
 pre_build:
   commands:
     - echo "pre_build step"
 build:
   commands:
     - aws s3 sync --delete . "arn:aws:s3:::midlife-coder.com/*
 post_build:
   commands:
     - echo "post_build step"
